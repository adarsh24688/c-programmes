#include <stdio.h>

int main()
{
	float length,width,area;
	printf("enter length");
	scanf("%d",&length);
	printf("enter width");
	scanf("%d",&width);
	 area=length*width;
	 printf("area is:%d",area);
	 return 0;
}
